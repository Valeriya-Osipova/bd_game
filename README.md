
## Запуск

Проект работает как обычная статическая веб-страница. Самый простой способ запустить его локально:

```bash
python -m http.server 8000
```

После этого откройте в браузере:

```text
http://127.0.0.1:8000/
```

## Управление

- Стрелки влево/вправо или A / D — движение по линии
- Пробел — наполнять ведро у колодца
- E — вылить воду в бочку

## Сборка и хостинг

Проект можно запустить локально и собрать в статическую папку `dist`.

```bash
npm install
npm run dev
```

Для подготовки релиза:

```bash
npm run build
```

Готовая сборка появится в папке `dist/` и подходит для статического хостинга (GitHub Pages, Netlify, Vercel и т.п.).

## Docker

Если хочешь упаковать игру в контейнер, добавлен `Dockerfile`:

```bash
docker build -t vedro-dlya-monashki .
```

Запуск контейнера:

```bash
docker run --rm -p 8080:80 vedro-dlya-monashki
```

После этого игра будет доступна по адресу `http://localhost:8080`.

## GitHub Pages

Этот репозиторий уже настроен на автоматический деплой через GitHub Actions.

После каждого пуша в ветку `master` GitHub Actions собирает проект и публикует папку `dist/` на Pages.

Если хочешь, можешь посмотреть статус деплоя в разделе `Actions` репозитория на GitHub.

> Обычно ссылка будет выглядеть как `https://Valeriya-Osipova.github.io/bd_game/`.

## Текущее состояние

- одна линия движения
- колодец и бочка на противоположных сторонах
- 10 вёдер на полную бочку
- простая анимация и минималистичный стиль
- фон загрузки из `assets/images/background`
- персонажная анимация из `assets/images/nan`
- звук воды из `assets/sounds/zvuk-stakan-vody.mp3`

## Что будет дальше

- улучшение анимации колодца и бочки
- звуки воды и ручки
- реплики и более атмосферный арт
